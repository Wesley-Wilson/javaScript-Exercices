/*for (let index = 15; index <= 200; index++){
    document.write("O valor do quadrado de "+ index + " é :" + index**2 + "</br>");
}*/

/*for(let contadora =1; contadora <= 11; contadora++){
    document.write(contadora+"</br>")
}

for (let index = 0; index < Array.length; index++) {
    const element = array[index];
}*/

/*let minuto = parseInt(0);
for (let segundo = 0; segundo < 60; segundo++){
    if (segundo == 60) {
        minuto = minuto + 1;
    }
    document.write(minuto + ":" + segundo + "</br>");
}*/

for (let hora = 0; hora <24; hora++){
    for (let minuto = 0; minuto < 60; minuto++) {
        for (let segundo = 0; segundo < 60; segundo++) {
            document.write(hora+":"+minuto+":"+segundo+"</br>");
        }
    }
}



// um terreno num campo de futebol para armazenar uma agulha. = variavel do tipo inteiro, caracter, float. 

// valores do mesmo tipo, homogenea, e no js podemos trabalhar com heterogenea, tipos diferentes.
// matrizes e uma dimensão ou vetores. array

// algoritmo e estrutura de dados. 

// Dois desafios para lista FOR
/* 
   1) Criar um cronometro com o laço de repetição for. 
   2) E depois um temporizador com o laço de repetição for.
   3) Criar um temporizador com o laço de repetição for, inserindo o gatilho inicial. 
*/


